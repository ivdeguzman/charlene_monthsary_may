import { writable } from "svelte/store";

export const switzerlandSelected = writable([0, 0]);
export const japanSelected = writable([0, 0]);
export const philippinesSelected = writable([0, 0]);
